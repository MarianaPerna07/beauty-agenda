from datetime import datetime

def get_available_slots(service_duration: int, worker_id: int, location_id: int, date: datetime) -> list:
    # This function should implement the logic to fetch available slots
    # based on the service_id, worker_id, location_id, and date_str.
    return []